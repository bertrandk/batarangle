/// <reference path="../../typings/tape/tape.d.ts"/>
import * as test from 'tape';
import { AdapterEvent , BaseAdapter } from '../../src/adapters/base';


test('adapters/angular2: #setup', t => {
  t.plan(2);

  t.skip('emits original root nodes');
  t.skip('emits original child nodes');
});

test('adapters/angular2: #traverseTree', t => {
  t.plan(8);

  t.skip('emits addition of root node');
  t.skip('emits addition of child node');
  t.skip('emits change of root node state');
  t.skip('emits change of root node properties');
  t.skip('emits change of child node state');
  t.skip('emits change of child node properties');
  t.skip('emits removal of root node');
  t.skip('emits removal of child node');
});